# NUT ups.conf — spartan (APC Back-UPS BX-1600MI)
# Driver: usbhid-ups (native USB HID, no serial adapter)

maxretry = 3

[apcups]
    driver = usbhid-ups
    port = auto
    desc = "APC Back-UPS BX-1600MI"
    pollinterval = 2
    ignorelb
